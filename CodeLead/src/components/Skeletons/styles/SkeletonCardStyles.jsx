import { Card } from "antd";
import styled from "styled-components";

export const SkeletonCard = styled(Card)`
  margin-bottom: 24px;
  border-radius: 8px;

  .ant-card-body {
    padding: 16px;
  }
`;
